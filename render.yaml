services:
  - type: web
    name: your-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DB_HOST
        value: your-hostinger-host
      - key: DB_USER
        value: your-username
      # Add all other environment variables